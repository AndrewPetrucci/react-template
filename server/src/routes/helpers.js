/**
 * Shared server helpers (non-schema). For schema helpers see schema/helpers.js.
 */

// Add common utilities here, e.g.:
// export function asyncHandler(fn) { return (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next) }
